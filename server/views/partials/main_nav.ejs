<nav class="navbar navbar-expand-md navbar-dark shadow" style="z-index:3; position: relative;">

  <div class="container-fluid">

    <a class="navbar-brand" href="/">
      <img src="/Assets/images/diliGentt_web.png" alt="" width="120" class="">
    </a>
    <ul class="nav navbar-nav justify-content-end navbar-collapse collapse" id="navbarNav">
      <% if (title=="Home" || title=="/" ){ %>
        <li class="nav-item active">
          <% } else {%>
        <li class="nav-item">
          <%} %>
            <a class="nav-link " href="/">Home <i class="fas fa-lg fa-laptop-house"></i></a>
        </li>

        <% if (title=="About Me" ){ %>
          <li class="nav-item active">
            <% } else {%>
          <li class="nav-item">
            <%} %>
              <a class="nav-link" href="/aboutme">About me <i class="fas fa-info-circle"></i></a>
          </li>

          <% if (title=="Projects" ){ %>
            <li class="nav-item active">
              <% } else {%>
            <li class="nav-item">
              <%} %>
                <a class="nav-link" href="/projects">Projects <i class="fas fa-thin fa-diagram-project"></i></a>
            </li>

            <% if (title=="Services" ){ %>
              <li class="nav-item active">
                <% } else {%>
              <li class="nav-item">
                <%} %>
                  <a class="nav-link" href="/services">Services <i class="fas fa-duotone fa-code"></i></a>
              </li>

              <% if (title=="Contact" ){ %>
                <li class="nav-item active">
                  <% } else {%>
                <li class="nav-item">
                  <%} %>
                    <a class="nav-link" href="/contactme">Contact <i class="fas fa-duotone fa-address-book"></i></a>
                </li>

                <% if (title=="Contact" ){ %>
                  <li class="nav-item active">
                    <% } else {%>
                  <li class="nav-item">
                    <%} %>
                      <a class="nav-link" href="/contactList">Contact List 
                        
                        <% if( loggedIn == true){ %>
                          <i id="contact-lock"class="fas fa-duotone fa-unlock"></i></a>
                        <%} else {%>

                          <i id="contact-lock"class="fas fa-duotone fa-lock"></i></a>
                        <%}%> 
                  </li>

                  <% if(typeof displayName == 'string' && displayName) { %>
                    <li class="navbar-text"><i class="fas fa-lg fa-user">        </i> Welcome, <%= displayName %></li>
                    <li class="nav-item"><a href="/logout" class="nav-link">
                      <i class="fas fa-lg fa-sign-out-alt"></i> Logout</a></li>
                  <% }%>
    </ul>

    <button onclick="fixContent()" id="burgerbutton" class="navbar-toggler" type="button" data-bs-toggle="collapse"
      data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

  </div>

</nav>

<script>
  function fixContent() {
    let navbarNav = document.getElementById('navbarNav');
    let burger = document.getElementById('burgerbutton');
    navbarNav.setAttribute('background-color:', 'rgba(10, 11, 34, 0.9)');
  }
</script>